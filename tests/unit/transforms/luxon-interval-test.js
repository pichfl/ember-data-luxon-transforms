import { moduleFor, test } from 'ember-qunit';

moduleFor('transform:luxon-interval', 'Unit | Transform | luxon interval', {});

// Replace this with your real tests.
test('it exists', function(assert) {
  let transform = this.subject();
  assert.ok(transform);
});
